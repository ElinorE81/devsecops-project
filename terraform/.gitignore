# Terraform state — contains sensitive data and is environment-specific
*.tfstate
*.tfstate.*

# Local variable overrides — may contain secrets
*.tfvars
!*.tfvars.example

# Terraform working directory
.terraform/
.terraform.lock.hcl

# Lambda build artifact — regenerated by Terraform's archive_file data source
lambda/*.zip
